# Devlog

### How to Start

```sh
# build client file
yarn
yarn build

# start entire server
yarn
yarn start

## if NODE_ENV is production, need to build first
## others, automatically build when start node server
```